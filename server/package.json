{
  "name": "angular-pilot",
  "version": "0.1.0",
  "description": "A starter project for AngularJS",
  "scripts": {
    "preinstall": "npm install -g grunt && npm install -g grunt-cli"
  },
  "dependencies": {
    "express": "^4.10.1",
    "nodemon": "^1.2.1"
  }
}
